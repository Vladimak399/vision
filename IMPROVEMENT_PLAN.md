# IMPROVEMENT_PLAN.md

## 1. Критичные исправления
| Что изменить | Зачем | Файлы | Риск | Как проверить |
|---|---|---|---|---|
| Запретить matching по одному слову и слабым совпадениям | Не подставлять похожий товар молча | `server/catalog-matching.ts`, `tests/catalog-matching.test.ts` | Средний: можно уменьшить авто-match и отправить больше строк на review | `npm test`, ручная проверка кандидатов |
| Усилить score через бренд, размер, упаковку, тип товара и варианты | Excel-каталог остается источником истины, меньше ложных совпадений | `server/catalog-matching.ts` | Средний: пороги могут потребовать настройки на реальных данных | Unit-тесты и sample monitoring |
| Сохранять статус отсутствия кандидата как отсутствие match | Не выдумывать товар | `server/auto-catalog-matching.ts` | Низкий: текущая логика уже близка | `npm test`, review UI |

## 2. Улучшение надежности
| Что изменить | Зачем | Файлы | Риск | Как проверить |
|---|---|---|---|---|
| Добавить test script и базовые unit-тесты | Предотвращать регрессии | `package.json`, `tests/*` | Низкий | `npm test` |
| Обновить `.env.example` под фактический код | Быстрый локальный запуск | `.env.example`, `README.md` | Низкий | Сверить с `lib/env.ts` |
| Задокументировать migration flow | Не ломать БД на deploy | `README.md`, `supabase/README.md` | Низкий | Документальная проверка |

## 3. Улучшение качества сопоставления товаров
| Что изменить | Зачем | Файлы | Риск | Как проверить |
|---|---|---|---|---|
| Нормализация текста с alias/stem для русского языка | Стабильнее сравнивать OCR и каталог | `server/catalog-matching.ts` | Средний: эвристики требуют данных | Unit-тесты normalize/match |
| Учет упаковки: пакет/банка/бутылка/коробка/дой-пак | Не путать одинаковые товары в разной упаковке | `server/catalog-matching.ts` | Низкий | Unit-тест упаковки |
| Different flavor same base как review reason | Бизнес-правило допускает цену другой разновидности, но не смену строки | `server/catalog-matching.ts` | Средний | Unit-тест аромата |

## 4. Улучшение работы с Excel
| Что изменить | Зачем | Файлы | Риск | Как проверить |
|---|---|---|---|---|
| В будущем добавить multi-sheet import для “Продукты” и “Химия” | Поддержать реальные файлы ассортимента | `app/app/catalog/actions.ts` | Средний | Fixture XLSX с двумя листами |
| В будущем добавить safe-template export через копию файла | Не ломать формулы/стили исходного мониторинга | Новая Excel service module | Средний | Integration-тест сравнения workbook structure |
| Не перезаписывать исходники | Защита данных | Export routes | Низкий: сейчас уже создается новый файл | Ручной export smoke test |

## 5. Улучшение интерфейса
| Что изменить | Зачем | Файлы | Риск | Как проверить |
|---|---|---|---|---|
| Показывать confidence/reasons около кандидата | Ревьюер понимает, почему выбран товар | `app/app/monitoring/[sessionId]/*` | Средний | UI smoke test |
| Явно показывать “Не найдено в ассортименте” | Не путать с ошибкой OCR | Review page | Низкий | Ручной сценарий unmatched |
| Для больших файлов добавить прогресс импорта | Не создавать ощущение зависания | Catalog import UI | Средний | Большой XLSX fixture |

## 6. Тесты и проверки
| Что изменить | Зачем | Файлы | Риск | Как проверить |
|---|---|---|---|---|
| Unit-тесты matching | Защитить бизнес-правила | `tests/catalog-matching.test.ts` | Низкий | `npm test` |
| Excel fixture tests | Проверить чтение/запись без потери структуры | Будущие `tests/excel*.test.ts` | Средний | `npm test` |
| Migration check | Убедиться в применимости SQL | Supabase CLI/local DB | Низкий | `supabase db reset`/`supabase db push` |

## 7. Финальная чистка проекта
| Что изменить | Зачем | Файлы | Риск | Как проверить |
|---|---|---|---|---|
| Обновить README/CHANGELOG/audit docs | Понятная эксплуатация проекта | `README.md`, `CHANGELOG.md`, `PROJECT_AUDIT.md` | Низкий | Документальная проверка |
| Запустить typecheck/build/tests | Финальная стабильность | Нет | Низкий | Команды из README |
